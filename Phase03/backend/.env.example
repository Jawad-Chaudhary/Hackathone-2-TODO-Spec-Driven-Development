# Database Configuration
# Get your connection string from Neon PostgreSQL dashboard
DATABASE_URL=postgresql+asyncpg://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# Authentication Configuration
# CRITICAL: This secret MUST match the frontend BETTER_AUTH_SECRET exactly
# Generate with: openssl rand -base64 48
BETTER_AUTH_SECRET=your-64-character-secret-here-replace-with-actual-secret-from-openssl

# CORS Configuration
# Comma-separated list of allowed origins (NO spaces)
# Development example: http://localhost:3000,http://localhost:3001
# Production example: https://your-app.vercel.app,https://your-app-staging.vercel.app
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Frontend URL Configuration (T040)
# Development: http://localhost:3000
# Production: https://your-app.vercel.app (replace with your actual Vercel URL)
FRONTEND_URL=http://localhost:3000

# Environment
ENVIRONMENT=development

# Logging
LOG_LEVEL=DEBUG

# AI Model Configuration
# Using Gemini 2.0 Flash via OpenAI-compatible API
# Get your API key from https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Legacy OpenAI Configuration (if switching back to OpenAI)
# OPENAI_API_KEY=sk-your-openai-api-key-here
